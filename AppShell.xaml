<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppGemini.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppGemini.Views"
    Title="AppGemini">

    <ShellContent Title="Chat" ContentTemplate="{DataTemplate local:ChatView}" />
    <ShellContent Title="Settings" ContentTemplate="{DataTemplate local:SettingsView}" />
</Shell>
